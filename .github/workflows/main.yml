name: Build LaTeX document
on: [push]
jobs:
  build_latex:
    runs-on: ubuntu-latest
    steps:
      - name: Set up Git repository
        uses: actions/checkout@v2
      - name: pdflatex main
        uses: dante-ev/latex-action@latest
        with:
          root_file: main.tex
          compiler: pdflatex
          args: -interaction=nonstopmode -shell-escape
      - name: bibtex conference
        uses: dante-ev/latex-action@latest
        with:
          root_file: conference.aux
          compiler: bibtex
          args: 
      - name: bibtex journal
        uses: dante-ev/latex-action@latest
        with:
          root_file: journal.aux
          compiler: bibtex
          args:
      - name: bibtex speeches
        uses: dante-ev/latex-action@latest
        with:
          root_file: speeches.aux
          compiler: bibtex
          args:
      - name: pdflatex main
        uses: dante-ev/latex-action@latest
        with:
          root_file: main.tex
          compiler: pdflatex
          args: -interaction=nonstopmode -shell-escape
